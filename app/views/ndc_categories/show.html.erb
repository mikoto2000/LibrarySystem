<p style="color: green"><%= notice %></p>

<h1><%= NdcCategory.model_name.human %></h1>

<%= render @ndc_category %>

<div>
  <%= link_to t("view.show.move_to_update", model: NdcCategory.model_name.human), edit_ndc_category_path(@ndc_category) %> |
  <%= link_to t("view.show.move_to_index", model: NdcCategory.model_name.human.pluralize), ndc_categories_path %>

  <%= button_to t("helpers.submit.delete"),
                @ndc_category,
                method: :delete,
                class: %w[btn btn-primary],
                data: { turbo_confirm: t("helpers.dialog.delete", id: @ndc_category.id) } %>
</div>
